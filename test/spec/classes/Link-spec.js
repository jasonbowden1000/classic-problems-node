let Link = require('./../../../classes/Link');
let expect = require('chai').expect;

describe('Link', () => {
  describe('when updating', () => {
    it('should only allow links as successors', () => {

    });
  });
});